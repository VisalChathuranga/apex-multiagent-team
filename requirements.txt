mcp>=1.0.0
filelock>=3.12.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Test dependencies
pytest>=8.0.0
httpx>=0.27.0
pytest-asyncio>=0.23.0
websockets>=12.0

# Optional — vector RAG (apex_semantic_search with chromadb backend)
# chromadb>=0.5.0
